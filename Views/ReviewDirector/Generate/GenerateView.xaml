<UserControl
        x:Class="Views.ReviewDirector.GenerateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:components="clr-namespace:SmartPrepModern.Components"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DesignHeight="720"
        d:DesignWidth="1280"
        mc:Ignorable="d">

        <Grid Background="#F5F7FA">
                <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1.2*" />
                </Grid.ColumnDefinitions>

                <components:TopicDiscoveryView x:Name="discoveryView" Grid.Column="0" />

                <Border
                        Grid.Column="0"
                        Width="1"
                        Margin="0,40"
                        HorizontalAlignment="Right"
                        Background="#E0E0E0" />

                <components:ExamConfigView x:Name="configView" Grid.Column="1" />
        </Grid>
</UserControl>
